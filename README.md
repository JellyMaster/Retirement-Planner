# Guided pension input flow patch

Copy the included `src` folder over the existing project `src` folder.

This update changes the main retirement-planner input experience to a five-step guided journey. Scenario comparison continues to use the existing comparison editor.

After applying, run:

```powershell
npm run verify
```
